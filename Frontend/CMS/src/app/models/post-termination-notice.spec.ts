import { PostTerminationNotice } from './post-termination-notice';

describe('PostTerminationNotice', () => {
  it('should create an instance', () => {
    expect(new PostTerminationNotice()).toBeTruthy();
  });
});
