import { AddAddendumContract } from './add-addendum-contract';

describe('AddAddendumContract', () => {
  it('should create an instance', () => {
    expect(new AddAddendumContract()).toBeTruthy();
  });
});
