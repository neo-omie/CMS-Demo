import { ContractTypeMaster } from './contract-type-master';

describe('ContractTypeMaster', () => {
  it('should create an instance', () => {
    expect(new ContractTypeMaster()).toBeTruthy();
  });
});
