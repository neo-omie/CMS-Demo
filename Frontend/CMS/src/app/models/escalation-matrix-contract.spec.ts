import { EscalationMatrixContract } from './escalation-matrix-contract';

describe('EscalationMatrixContract', () => {
  it('should create an instance', () => {
    expect(new EscalationMatrixContract()).toBeTruthy();
  });
});
