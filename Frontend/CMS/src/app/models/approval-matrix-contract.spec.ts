import { ApprovalMatrixContract } from './approval-matrix-contract';

describe('ApprovalMatrixContract', () => {
  it('should create an instance', () => {
    expect(new ApprovalMatrixContract()).toBeTruthy();
  });
});
